# Sync Repeaters Workflow

name: Sync Repeaters

on:
  schedule:
    - cron: '0 * * * *'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Sync Repeaters
        run: |
          curl -X GET https://yow.meshmapper.net/get_repeaters.php -o repeaters.json
          # Additional commands to process repeaters.json
