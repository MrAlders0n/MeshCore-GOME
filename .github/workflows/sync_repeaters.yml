# This workflow will sync repeaters

name: Sync Repeaters

on:
  schedule:
    - cron: '0 * * * *'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Fetch repeaters
        run: |
          curl -X GET https://yow.meshmapper.net/get_repeaters.php -o repeaters.json

      - name: Update mesh mapping
        run: |
          # Add commands to update mapping based on repeaters.json
